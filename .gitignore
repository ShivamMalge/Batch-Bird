/target

# Synthetic benchmark data — millions of rows, regenerated deterministically by bench/.
/data

# Criterion's working directory. Exported CSV timings are committed deliberately; the
# raw sample files and generated HTML are not.
/target/criterion
